//No tiene caso validar el login
/*export const valLogin = (req,res, next)=>
{
    const {usuario, password}=req.body;
    if(!validacion)
        res.status(400).send('Credenciales invalidas');
    
}

function validacion(usuario, password)
{
    if(typeof usuario==="string"&&usuario.trim().length!==0&&typeof password==="string"&&password.trim().length!==0)
        return true;
    else
        return false;
}*/